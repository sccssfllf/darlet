# Darlet Compiler

**Compiler is still in work: The version of compiler with English comments and description will be available soon.**

Darlet — це навчальний компілятор та інтерпретатор власної мови програмування, написаний на C#. Проєкт реалізує повний цикл роботи компілятора: від лексичного аналізу до виконання коду.

## 🏗 Архітектура

Проєкт складається з наступних модулів:

- **Core/LexicalAnalysis**: Лексер (Tokenization). Перетворює вихідний код на потік токенів.
- **Core/SyntaxAnalysis**: Парсер (Recursive Descent Parser). Будує абстрактне синтаксичне дерево (AST).
- **Core/SyntaxAnalysis/AST**: Визначення вузлів дерева (BinOp, Number, If, While тощо).
- **Core/SemanticAnalysis**: Таблиця символів та перевірка областей видимості (Scopes).
- **Core/Errors**: Обробка помилок компіляції.

## 🚀 Як запустити

1. Клонуйте репозиторій:

   ```bash
   git clone https://github.com/sccssfllf/darlet.git
   ```

2. Відкрийте проєкт у Visual Studio або VS Code.

3. Збілдіть рішення:

   ```bash
   dotnet build
   ```

4. Запустіть проєкт у консолі(якщо є консольний інтерфейс у Program.cs).
   ```bash
   dotnet run --project darlet.CLI
   ```

## 📝 Приклад синтаксису (Darlet)

```js
var x = 10;
var y = 20;

if (x < y) {
  print(x + y);
} else {
  print(0);
}

var counter = 0;
while (counter < 5) {
  print(counter);
  counter = counter + 1;
}
```

## 🛠 Технології

- C# (.NET)
- LINQ
